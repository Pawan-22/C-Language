#include <stdio.h>

int main()
{
    int a = 4; 
    int b = 32;
    b += a; // or b = b + a
    // b -= a is or b = b - a 
    // b *= a is or b = b * a 
    // b /= a is or b = b / a 

    printf("Value of b is %d \n",b);
 
	return 0;
}
